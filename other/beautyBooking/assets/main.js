$(document).ready(function () {
  $('.services-carousel').owlCarousel({
    loop: false,
    margin: 16,
    nav: false,
    autoWidth: true,
  });

  $('.sertificates-carousel').owlCarousel({
    loop: true,
    margin: 16,
    nav: false,
    autoWidth: true,
  });
})